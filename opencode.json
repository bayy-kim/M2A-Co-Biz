{
  "$schema": "https://opencode.ai/config.json",
  "command": {
    "build": {
      "template": "Run `npm run build` locally to check for build or compilation errors.",
      "description": "Build Next.js application"
    },
    "seed": {
      "template": "Run `npm run seed` to seed local database mock credentials. Safe for local deployment.",
      "description": "Seed database"
    }
  }
}