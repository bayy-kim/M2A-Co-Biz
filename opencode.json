{
  "$schema": "https://opencode.ai/config.json",
  "command": {
    "build": {
      "template": "Run `npm run build` and fix any TypeScript or build errors that appear.",
      "description": "Build project & fix errors"
    },
    "seed": {
      "template": "Run `npm run seed` locally to seed the database with test data. NEVER run this against production — seeding must always be a manual, local-only step.",
      "description": "Seed database (manual only)"
    }
  }
}